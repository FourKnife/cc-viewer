# M1.1: UI 优化 — 归档路线图

**版本**: v1.1
**状态**: 已完成
**时间**: 2026-04-22
**Phases**: 2 (Phase 7-8)

---

## Phase 7: 选中元素 Tag + 状态栏

**目标**: 优化元素选中交互 + 新增全局状态栏

**任务**:
- [x] ChatView 输入框新增「选中元素」Tag 组件
- [x] Tag 显示元素摘要（标签名 + class 片段），可关闭
- [x] 发送时自动注入选中元素上下文（替代旧的 "AI修改此元素" 按钮）
- [x] 删除 ElementInfo 中的 askAI 按钮和相关逻辑
- [x] 新增 StatusBar 组件（Sketch MCP 状态 + 图层名 + 选中元素 class）
- [x] Sketch MCP 连接检测 + 心跳
- [x] 集成 StatusBar 到 VisualEditor 布局

**关键文件**:
- 新建: `cc-viewer/src/components/VisualEditor/StatusBar.jsx`
- 修改: `ChatView.jsx`, `App.jsx`, `AppBase.jsx`, `ElementInfo.jsx`, `styles.module.css`

**决策**:
- Tag 以 monospace 字体渲染在 ChatView textarea 上方
- buildElementContext 自动将源码信息前置注入用户消息
- Sketch MCP 心跳使用 no-cors HEAD 请求 localhost:31126，间隔 15s
- StatusBar 放在 contentVisual flex 列底部

## Phase 8: 截图对比 POC + UI 质量优化

**目标**: 验证 iframe-Sketch 截图对比可行性 + 整体 UI 打磨

**任务**:
- [x] 调研 iframe 截图方案（chrome MCP / html2canvas）
- [x] 调研 Sketch MCP 图层导出能力
- [x] 实现截图对比原型（如可行）
- [x] 审视现有 UI，去除 AI slop 元素
- [x] 优化视觉层次、间距、颜色一致性
- [x] 优化空状态和 loading 体验

**交付物**:
- 截图对比可行性报告
- UI 质量提升

---

## 关键成就

1. **选中元素 Tag 交互** — 选中页面元素后自动在输入框显示 Tag，发送时自动注入上下文
2. **全局状态栏** — 展示 Sketch MCP 连接状态、图层名、选中元素 class
3. **截图对比 POC** — 验证 iframe 截图与 Sketch 设计稿对比的可行性
4. **UI 质量打磨** — 去除 AI slop 元素，优化视觉层次和一致性
